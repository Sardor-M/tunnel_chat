{
  "name": "@tunnel_chat/common",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --watch --dts",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@tunnel_chat/eslint-config": "workspace:*",
    "@tunnel_chat/typescript-config": "workspace:*",
    "tsup": "^7.0.0",
    "typescript": "^5.0.0"
  }
}
